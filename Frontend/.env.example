# Leave empty in local dev to use Vite proxy (/api -> http://127.0.0.1:5050).
# Set this to your deployed backend URL in production, for example:
# VITE_API_BASE_URL=https://your-backend-domain.com
VITE_API_BASE_URL=